[package]
name = "rain-sim"
version = "0.1.0"
edition = "2021"
description = "Off-main-thread water-map droplet simulation for rain-edit"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
